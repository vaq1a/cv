export const MIN_LOGIN_LENGTH = 5;
export const MIN_PASSWORD_LENGTH = 10;

export const LOGIN_MIN_MESSAGE = `Username must be at least ${MIN_LOGIN_LENGTH} characters.`;
export const PASSWORD_MIN_MESSAGE = `Password must be at least ${MIN_PASSWORD_LENGTH} characters.`;
